# Wrapper for data extraction
import subprocess
import os

def run_extraction():
    print("Starting data extraction...")
    # This would normally call logic from src/extraction
    # For now, we point to the logic implemented in the src folders
    print("Logic located in src/extraction/")

if __name__ == "__main__":
    run_extraction()
